// Temporarily unused. Might be used in the future.
import { extendTheme } from "@chakra-ui/react";

const Theme = extendTheme({
    fonts: {
        heading: `'League Spartan', sans-serif`,
        body: `'DM Sans', sans-serif`
    }
});

export default Theme;